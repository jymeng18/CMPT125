#include <stdio.h>


int bubbleSort(int * arr, int size){
    for(int i = 0; i < size - 1; i++){
        for(int j = 0; j < size - 1 - i; j++){
            if(arr[j+1] < arr[j])
        }
    }
}

int main(void){

    int arr1[] = {2, 3, 4, 5};
    printArray(arr1, 0, sizeof(arr1) / sizeof(arr1[0]));
    return 0;
}